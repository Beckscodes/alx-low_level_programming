#include "main.h"

/**
 * mul - function that multiplies 2 integers
 *
 * @a: a - variable
 * @a: b - variable
 * Return: 0 Always (Success)
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
