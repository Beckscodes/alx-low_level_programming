#include "main.h"

/**
 * mul - function that multiplies 2 integers
 *
 * @x: x - variable
 * @y: y - variable
 * Return: 0 Always (Success)
 */
int mul(int x, int y)
{
	int mul;

	mul = x * Y;

	return (mul);
}
